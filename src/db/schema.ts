// Drizzle schema lives here. Tables (users, identities, admins, campaigns,
// applications, …) are defined in Phase 1 (data layer) per the build plan §7.
// Intentionally empty for the Phase 0 scaffold so the DB tooling is wired
// without committing to any schema yet.
export {};
